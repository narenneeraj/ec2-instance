pipeline {
agent any
  stages {
  
    stage('test'){
      steps {
      sh '''
      whoami
      aws configure
      sudo terraform init
      sudo terraform apply --auto-approve
      
      '''
      }
      }
    }

}
